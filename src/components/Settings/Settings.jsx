import React from "react";
import './Settings.scss';
import Tooltip from './TestComponent'
const Settings = (props) => {
    return (
        <div className = 'Settings__page'>
            <Tooltip />
        </div>
    )
}

export default Settings;